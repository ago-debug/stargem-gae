# REPORT AUDIT DELTA DB 25→28 APRILE
**Riferimento Protocollo:** F1-PROTOCOLLO-001
**Data:** 28/04/2026

## 1. SOMMARIO ESECUTIVO
L'indagine ha rivelato che il delta non è frutto di lente modifiche organiche UI, ma di **una massiva esecuzione di script diretti sul Database (fuori dalle route applicative)** avvenuta nella giornata del 27 Aprile (ora locale italiana pomeriggio/sera).
- Tutte le `enrollments` e i `payments` preesistenti sono stati cancellati (WIPE) e rigenerati da zero in blocco.
- Sono stati aggiunti centinaia di `members` (da 4.489 a 4.918).
- L'assenza totale di tracce nei log applicativi conferma l'uso di tool esterni o script diretti via MySQL (`super_migration.js`, `sync_bitrix.js`).

## 2. ORIGINE DEI DELTA (SEZIONE A)
- **`enrollments` (−1.350):** Tutti i 12.234 record attuali hanno un `created_at` datato 27 Aprile (~18:00 ora locale). I 13.584 record precedenti sono stati droppati. Il campo `source_file` è `NULL` per tutti, cancellando così le preziose tracce ("gsheets_master", "athena", ecc) che avevamo.
- **`payments` (+8.287):** Come sopra. Tutti i 12.062 record attuali sono stati iniettati in batch il 27 Aprile. Il campo `source` è stato settato brutalmente a `'sede'` per tutti.
- **`members` (+429):** Sono stati inseriti 576 nuovi record (compensando eventuali drop) sempre in batch il 27 Aprile. Il campo `from_where` è `NULL`.
- **`courses` (+16):** Sono stati creati 19 nuovi corsi tra le 10:00 e le 12:00 del 28 Aprile. 

## 3. ATTRIBUZIONE OPERAZIONI (SEZIONE B)
- **Log Applicativi:** `audit_logs` e `user_activity_logs` **NON** riportano alcuna traccia di questi 20.000+ inserimenti. Questo dimostra matematicamente che l'operazione non è passata dall'UI / Express API.
- **Script di Progetto:** L'ispezione della root ha rivelato l'esistenza e la modifica (il 27 Aprile pomeriggio) di script Node.js eseguiti localmente:
  - `super_migration.js`
  - `update_members_data.js`
  - `sync_bitrix.js` e `sync_bitrix_v2.js`
Questi script contengono logica di sincronizzazione massiva.

## 4. RICONCILIAZIONE BACKUP (SEZIONE C)
- Come indicato in `Z_REPORT_CLEANUP_DB.md`, il backup `enrollments_backup_op1235_2026_04_26_09_4` conteneva esattamente 13.581 record (confermando la base pre-wipe). 
- Nessun backup completo in locale con data 27-28 Aprile. I backup risiedono in `/root/backups/` sul VPS come da direttive `MASTER_STATUS.md`.

## 5. ATTRIBUZIONE A CHAT (SEZIONE D)
Il delta massivo **NON** è attribuibile a nessuna chat operativa standard in corso.
- Nel `MASTER_STATUS.md` si legge: **"Chat_06_Contabilità: Rollback import pagamenti ancora pendente (ALTA PRIORITÀ)"** e *"Smart Routing import: i pagamenti NON passano più per enrollments per errore"*.
- È evidente che al di fuori dei perimetri protetti, qualcuno (Gaetano o uno script batch legacy) ha lanciato una sincronizzazione Bitrix (`sync_bitrix_v2.js`) che ha causato un travaso enorme di pagamenti pregressi (da qui i +8.287), rasando e ricostruendo le `enrollments` senza i metadati originali (`source_file`).

**ANOMALIA CRITICA:** L'operazione ha distrutto la colonna `source_file` su `enrollments` rendendo anonime le iscrizioni (tutto NULL).

## 6. IMPATTO SULLA CHECKLIST CHAT_08 (SEZIONE E)
- **Filtro Categoria / Performance / Participation Type:** Non cambiano.
- **Campi nascosti (source_file, notes, season_id):** IMPATTO ALTO. Il campo `source_file` non contiene più i preziosi identificatori dell'import originario.
- **Allineamento SKU / Bonifica:** Rischio medio. I 19 nuovi corsi creati il 28 Aprile potrebbero aver reintrodotto vecchie sigle o `activity_type` in italiano. Richiede una verifica in corsa.

## 7. RACCOMANDAZIONI OPERATIVE
Non toccare le logiche di `courses.tsx` prima di aver:
1. **Deciso se fare il Rollback:** Il DB attuale ha perso preziose informazioni storiche sulle iscrizioni a fronte dell'import Bitrix massivo. Se il rollback menzionato in Chat_06 avverrà, lavorare ora sui Corsi è inutile perché i dati cambieranno di nuovo.
2. **Ricontrollare i 19 Corsi Nuovi:** Verificare se rispettano lo standard STI (no sigle storiche).

---

### TABELLA RIEPILOGATIVA

| Tabella | Delta | Fonte attribuita | Confidenza | Note |
|---|---|---|---|---|
| `enrollments` | −1.350 | Script `super_migration.js` / Bitrix Sync | ALTA | Wipe totale + Insert. Persi i `source_file`. |
| `payments` | +8.287 | Script Bitrix Sync (`sync_bitrix_v2.js`) | ALTA | Inserimento massivo, tutti `source: 'sede'`. |
| `members` | +429 | Script `super_migration.js` | ALTA | Nessuna traccia API/UI. |
| `courses` | +16 | Uso UI Manuale (28 Aprile) | MEDIA | Creati orario lavorativo, logica organica. |
